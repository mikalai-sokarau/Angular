import { BorderStylingDirective } from './borderStyling/border-styling.directive';

describe('BorderStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderStylingDirective();
    expect(directive).toBeTruthy();
  });
});
